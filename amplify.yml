version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 20 || true
        - nvm use 20 || true
        - node -v
        - corepack enable
        - corepack prepare pnpm@9 --activate
        # 👇 ensure devDependencies (tailwind/postcss) are installed
        - pnpm install --frozen-lockfile --prod=false
    build:
      commands:
        # Build only the site-renderer app
        - pnpm --filter site-renderer build
  artifacts:
    baseDirectory: apps/site-renderer/.next
    files:
      - '**/*'
  cache:
    paths:
      - .pnpm-store/**/*
      - node_modules/**/*
      - apps/site-renderer/.next/cache/**/*
