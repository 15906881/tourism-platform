name: API
permissions:
  contents: read
on:
  workflow_dispatch: {}
  push:
    branches: [develop, main]
    paths: ['api/**', 'scripts/**', '.github/workflows/api.yml']
jobs:
  api-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: echo "API workflow placeholder - add actual tests when API is implemented"
