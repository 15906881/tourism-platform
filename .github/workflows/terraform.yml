# DISABLED: AWS authentication issues  
# This workflow is disabled until AWS OIDC authentication is configured
name: Terraform

on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'Terraform disabled - AWS auth needed'
        required: false

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow Disabled
        run: |
          echo "❌ Terraform workflow is disabled"
          echo "🔧 Fix AWS authentication to enable"
          exit 1
