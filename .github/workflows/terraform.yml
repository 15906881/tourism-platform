name: Terraform
# DISABLED: AWS authentication issues
# on:
#   push:
#     branches: [main]
#   workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Terraform workflow disabled - fix AWS authentication first"
