phase-2-terraform/
  environments/prod/
    main.tf
    variables.tf
    terraform.tfvars.example   # prefilled with your VPC/subnet IDs, region, repo, port 8080
  modules/
    alb/            # ALB + TG + SG
    ecs_service/    # Cluster, TaskDef, Service, IAM, SG, Logs
    rds_postgres/   # Toggle create_db=true later; private subnets + SG
  README.md
