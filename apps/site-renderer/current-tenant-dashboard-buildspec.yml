
version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 20.19.5 || true
        - nvm use 20.19.5 || true
        - corepack enable
        - pnpm install --frozen-lockfile --prod=false
    build:
      commands:
        - pnpm --filter @weblynk/tokens build
        - pnpm --filter @weblynk/ui build
        - pnpm --filter @weblynk/blocks build
        - pnpm --filter @weblynk/templates build
        - pnpm --filter tenant-dashboard build
        # REMOVED: - pnpm --filter tenant-dashboard export
  artifacts:
    baseDirectory: apps/tenant-dashboard/out
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
      - .pn-store/**/*
      - apps/**/.next/cache/**/*
      - packages/**/dist/**/*

